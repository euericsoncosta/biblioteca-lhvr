<div class="mb-8">
    <a href="/livros" class="inline-flex items-center gap-2 text-slate-400 hover:text-indigo-600 font-bold text-xs uppercase tracking-widest mb-4 transition-colors">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
        </svg>
        Voltar ao Acervo
    </a>
    <h2 class="text-3xl font-black text-slate-800 dark:text-white tracking-tight">Catalogar Novo Livro</h2>
    <p class="text-slate-500 dark:text-slate-400 font-medium">Preencha os dados da obra para integrá-la ao sistema da Biblioteca LHVR.</p>
</div>

{{#if errors}}
    <div class="mb-6 p-4 bg-rose-50 dark:bg-rose-900/20 border border-rose-100 dark:border-rose-800 rounded-2xl">
        <ul class="list-disc list-inside text-rose-600 dark:text-rose-400 text-sm font-bold">
            {{#each errors}}
                <li>{{this}}</li>
            {{/each}}
        </ul>
    </div>
{{/if}}

<div class="max-w-4xl bg-white dark:bg-slate-800 rounded-[2.5rem] shadow-sm border border-slate-100 dark:border-slate-700 p-8 md:p-12 transition-all">
    <form action="/livros/store" method="POST" class="space-y-8">
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <div class="md:col-span-2">
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Título da Obra
                </label>
                <input type="text" name="nome" value="{{livro.nome}}" required placeholder="Ex: Dom Quixote"
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>

            <div>
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Autor
                </label>
                <input type="text" name="autor" value="{{livro.autor}}" required placeholder="Nome do autor" 
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>

            <div>
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Número de Tombo (Sugerido: {{proximoTombo}})
                </label>
                <input type="text" name="tombo" value="{{#if livro.tombo}}{{livro.tombo}}{{else}}{{proximoTombo}}{{/if}}" required placeholder="Ex: 5718" 
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white font-mono uppercase font-bold text-indigo-600 dark:text-indigo-400">
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div>
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Ano de Lançamento
                </label>
                <input type="number" name="ano" value="{{livro.ano}}" placeholder="2024" 
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>

            <div>
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Editora
                </label>
                <input type="text" name="editora" value="{{livro.editora}}" placeholder="Nome da editora" 
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>

            <div>
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Categoria
                </label>
                <input type="text" name="categoria" value="{{livro.categoria}}" placeholder="Ex: Literatura" 
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>
        </div>

        <div class="pt-8 border-t border-slate-50 dark:border-slate-700 flex justify-end gap-4">
            <button type="submit" class="px-10 py-4 bg-indigo-600 text-white rounded-2xl font-black shadow-xl shadow-indigo-100 dark:shadow-none hover:bg-indigo-700 active:scale-95 transition-all">
                Salvar Livro
            </button>
        </div>
    </form>
</div>