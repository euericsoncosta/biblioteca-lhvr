<div class="mb-8">
    <a href="/livros" class="inline-flex items-center gap-2 text-slate-400 hover:text-indigo-600 font-bold text-sm mb-4 transition-colors">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
        </svg>
        Cancelar Edição
    </a>

    <div class="flex items-center gap-4">
        <h2 class="text-3xl font-black text-slate-800 dark:text-white tracking-tight">Editar Obra</h2>
        <span class="px-3 py-1 bg-slate-100 dark:bg-slate-700 text-slate-500 dark:text-slate-400 rounded-lg text-[10px] font-black uppercase">
            ID: {{livro.id}}
        </span>
    </div>
    <p class="text-slate-500 dark:text-slate-400 font-medium">Atualize os dados técnicos ou o estado de disponibilidade do livro.</p>
</div>

<div class="max-w-4xl bg-white dark:bg-slate-800 rounded-[2.5rem] shadow-sm border border-slate-100 dark:border-slate-700 p-8 md:p-12">
    <form action="/livros/update/{{livro.id}}" method="POST" class="space-y-8">
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="md:col-span-2">
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Título do Livro
                </label>
                <input type="text" name="nome" value="{{livro.nome}}" required
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>

            <div>
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Autor
                </label>
                <input type="text" name="autor" value="{{livro.autor}}" required
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>

            <div>
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Número de Tombo
                </label>
                <input type="text" name="tombo" value="{{livro.tombo}}" required
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white font-mono">
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <div>
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Ano
                </label>
                <input type="number" name="ano" value="{{livro.ano}}"
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>

            <div class="lg:col-span-2">
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Categoria
                </label>
                <input type="text" name="categoria" value="{{livro.categoria}}"
                    class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
            </div>

            <div class="relative">
                <label class="block text-xs font-black uppercase text-slate-400 dark:text-slate-500 mb-2 ml-1 tracking-widest">
                    Estado Atual
                </label>
                <select name="status" class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white appearance-none cursor-pointer">
                    <option value="disponivel" {{#if (eq livro.status 'disponivel')}}selected{{/if}}>Disponível</option>
                    <option value="emprestado" {{#if (eq livro.status 'emprestado')}}selected{{/if}}>Emprestado</option>
                    <option value="reservado" {{#if (eq livro.status 'reservado')}}selected{{/if}}>Reservado</option>
                    <option value="manutencao" {{#if (eq livro.status 'manutencao')}}selected{{/if}}>Em Manutenção</option>
                </select>
                <div class="absolute right-5 top-[3.2rem] pointer-events-none text-slate-400">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                    </svg>
                </div>
            </div>
        </div>

        <div class="pt-6 border-t border-slate-50 dark:border-slate-700 flex justify-end gap-4">
            <button type="submit" class="w-full md:w-auto px-12 py-4 bg-indigo-600 text-white rounded-2xl font-black shadow-xl shadow-indigo-100 dark:shadow-none hover:bg-indigo-700 active:scale-95 transition-all">
                Guardar Alterações
            </button>
        </div>
    </form>
</div>