<!DOCTYPE html>
<html lang="pt-pt">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Conta - Biblioteca LHVR</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;900&display=swap" rel="stylesheet">
    
    <script>
        tailwind.config = { darkMode: 'class' }
    </script>
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
            transition: all 0.3s;
        }

        :root {
            --base-font-size: 16px;
        }

        html {
            font-size: var(--base-font-size);
        }
    </style>
</head>

<body class="bg-slate-50 dark:bg-slate-900 text-slate-900 dark:text-slate-100 min-h-screen flex items-center justify-center p-6">

    <div class="w-full max-w-xl bg-white dark:bg-slate-800 p-10 rounded-[2.5rem] shadow-2xl border border-slate-100 dark:border-slate-700 transition-all">
        
        <div class="mb-8 text-center">
            <h2 class="text-3xl font-black text-slate-800 dark:text-white tracking-tight">Criar Nova Conta</h2>
            <p class="text-slate-400 dark:text-slate-500 font-medium">Registo exclusivo para alunos da LHVR.</p>
        </div>

        {{#if error}}
            <div class="mb-6 p-4 bg-rose-50 dark:bg-rose-900/20 border border-rose-100 dark:border-rose-800 rounded-2xl text-rose-600 dark:text-rose-400 text-sm font-bold">
                ⚠️ {{error}}
            </div>
        {{/if}}

        <form action="/signup" method="POST" class="space-y-5">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                
                <div class="md:col-span-2">
                    <label class="block text-xs font-black uppercase text-slate-400 mb-2 ml-1 tracking-widest">Nome Completo</label>
                    <input type="text" name="nome" required placeholder="Seu nome" 
                        class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
                </div>

                <div>
                    <label class="block text-xs font-black uppercase text-slate-400 mb-2 ml-1 tracking-widest">E-mail</label>
                    <input type="email" name="email" required placeholder="aluno@escola.pt" 
                        class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
                </div>

                <div>
                    <label class="block text-xs font-black uppercase text-slate-400 mb-2 ml-1 tracking-widest">Senha</label>
                    <input type="password" name="senha" required placeholder="••••••••" 
                        class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white">
                </div>

                <div>
                    <label class="block text-xs font-black uppercase text-slate-400 mb-2 ml-1 tracking-widest">Matrícula</label>
                    <input type="text" name="documento" required placeholder="Nº de Aluno" 
                        class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white font-mono">
                </div>

                <div class="relative">
                    <label class="block text-xs font-black uppercase text-slate-400 mb-2 ml-1 tracking-widest">Curso</label>
                    <select name="curso" required 
                        class="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-600 transition-all dark:text-white appearance-none cursor-pointer">
                        <option value="" disabled selected>Selecione o seu curso</option>
                        <option value="Enfermagem">Enfermagem</option>
                        <option value="Informatica">Informática</option>
                        <option value="Finanças">Finanças</option>
                        <option value="Segurança do trabalho">Segurança do Trabalho</option>
                        <option value="Agronegocio">Agronegócio</option>
                    </select>
                    <div class="absolute right-5 top-[3.2rem] pointer-events-none text-slate-400">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                        </svg>
                    </div>
                </div>
            </div>

            <button type="submit" class="w-full bg-indigo-600 text-white py-4 rounded-2xl font-black text-lg shadow-xl shadow-indigo-100 dark:shadow-none hover:bg-indigo-700 active:scale-95 transition-all">
                Criar Conta
            </button>

            <div class="text-center pt-4">
                <p class="text-sm text-slate-400">
                    Já tem uma conta? 
                    <a href="/login" class="text-indigo-600 font-black hover:underline underline-offset-4">Faça Login</a>
                </p>
            </div>
        </form>
    </div>

</body>
</html>