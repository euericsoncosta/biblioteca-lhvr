<!DOCTYPE html>
<html lang="pt-pt">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erro - Biblioteca LHVR</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;900&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            darkMode: 'class'
        }
    </script>

    <style>
        body {
            font-family: 'Inter', sans-serif;
            transition: all 0.3s;
        }

        :root {
            --base-font-size: 16px;
        }

        html {
            font-size: var(--base-font-size);
        }
    </style>
</head>

<body class="bg-slate-50 dark:bg-slate-900 text-slate-900 dark:text-slate-100 h-screen flex items-center justify-center p-4">

    <div class="w-full max-w-lg text-center bg-white dark:bg-slate-800 p-10 rounded-[2.5rem] shadow-2xl border border-slate-100 dark:border-slate-700">
        
        <div class="bg-rose-100 dark:bg-rose-900/30 w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-6">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-rose-600 dark:text-rose-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
        </div>

        <h1 class="text-4xl font-black text-slate-800 dark:text-white mb-4">Ups! Algo correu mal.</h1>

        <div class="p-4 bg-slate-50 dark:bg-slate-900 rounded-2xl mb-8">
            <p class="text-slate-600 dark:text-slate-400 font-medium">
                {{#if message}}
                    {{message}}
                {{else}}
                    Ocorreu um erro inesperado no servidor.
                {{/if}}
            </p>
        </div>

        <div class="flex flex-col sm:flex-row gap-3">
            <a href="/" class="flex-1 bg-indigo-600 text-white py-4 rounded-2xl font-black shadow-xl shadow-indigo-100 dark:shadow-none hover:bg-indigo-700 transition-all">
                Voltar ao Início
            </a>
            <button onclick="window.history.back()" class="flex-1 bg-slate-100 dark:bg-slate-700 text-slate-600 dark:text-slate-300 py-4 rounded-2xl font-black hover:bg-slate-200 dark:hover:bg-slate-600 transition-all">
                Tentar Novamente
            </button>
        </div>

        <p class="mt-8 text-xs text-slate-400 dark:text-slate-500 uppercase font-bold tracking-widest">
            Biblioteca LHVR - Sistema de Gestão
        </p>
    </div>

    <script>
        // Aplicar o tema guardado e o tamanho da fonte ao carregar
        window.onload = () => {
            const theme = localStorage.getItem('theme') || 'light';
            document.documentElement.classList.toggle('dark', theme === 'dark');
            
            const size = localStorage.getItem('fontSize') || 16;
            document.documentElement.style.setProperty('--base-font-size', size + 'px');
        };
    </script>

</body>

</html>